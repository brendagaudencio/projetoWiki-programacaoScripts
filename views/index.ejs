<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.4/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <!-- Tema OuiWine -->
    <style>
        .navbar { background-color: #170000 !important; }
        .footer { background-color: #170000; }
        .hero-section {
            background: linear-gradient(rgba(23, 0, 0, 0.7), rgba(23, 0, 0, 0.7)),
                        url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 800"><rect fill="%23440000" width="1200" height="800"/></svg>');
            background-size: cover;
            min-height: 60vh;
            color: white;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">
                <i class="bi bi-cup-straw me-2"></i>
                OuiWine
            </a>
            
            <div class="navbar-nav ms-auto">
                <a class="btn btn-danger btn-sm" href="/login">Login</a>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero-section d-flex align-items-center">
        <div class="container">
            <div class="row">
                <div class="col-lg-8 mx-auto text-center">
                    <h1 class="display-4 fw-bold mb-4">
                        <%= pageTitle %>
                    </h1>
                    <p class="lead mb-4">
                        Backend Express + EJS funcionando!
                    </p>
                    <div class="alert alert-success" role="alert">
                        <h4 class="alert-heading">🎉 Sucesso!</h4>
                        <p>
                            Aplicação Fullstack <strong>funcionando</strong>! 
                            Página funcionado dinamicamente.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer text-center py-4 text-light">
        <div class="container">
            <p class="mb-0">
                🍷 OuiWine - Versão 2.0 Fullstack
            </p>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.4/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
